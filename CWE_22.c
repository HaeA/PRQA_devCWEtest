//test

int main() {

	char* fname = "../abc";

	f = open(fname);
	for (int i = 0; i < 10; i++) {
		arr[ind+9] = ind++;
		arr[cat] = ind;
		cat++;
		cat--;
		arr[cat++] = ind;
		arr[3] = ind + cat;
		/* check operation
		 * test
		 */
		
	}
	
	memset(passwd, 0, sizeof(passwd));
	free(pt);
	return;
}
char glb3;	